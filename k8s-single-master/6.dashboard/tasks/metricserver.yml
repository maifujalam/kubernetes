- name: Copy metric-server.yaml to /var/iac/{{cluster_name}}
  template:
    src: metrics-server.yaml.j2
    dest: /var/iac/{{cluster_name}}/metrics-server.yaml

- name: Install Kubernetes Metrics-server
  command: kubectl apply -f /var/iac/{{cluster_name}}/metrics-server.yaml
